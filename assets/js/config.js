/* ============================================================
   StreetFood OS — ตั้งค่าการเชื่อมต่อ
   ------------------------------------------------------------
   ปล่อยค่าว่างไว้ = ทำงานในโหมดเดโม (Mock Data ในเครื่อง ไม่ต่อเน็ต)
   ใส่ค่า            = ทำงานในโหมดจริง (อ่าน/เขียน Supabase)

   ⚠️ anonKey เปิดเผยในหน้าเว็บได้โดยการออกแบบ — มันไม่ใช่ความลับ
      สิ่งที่กันข้อมูลคือ RLS ใน 03_rls.sql ไม่ใช่การซ่อน key
      ห้ามใส่ service_role key ที่นี่เด็ดขาด (ตัวนั้นข้าม RLS ทั้งหมด)
   ============================================================ */
window.SFOS_CONFIG = {
  supabaseUrl: 'https://maezclcrarmqsgtoieas.supabase.co',
  supabaseAnonKey: '',    // ⬅️ ใส่ anon / public key จาก Project Settings → API Keys
  storeId: ''             // ปล่อยว่างไว้ = ใช้ร้านแรกที่ผู้ใช้เป็นสมาชิก
};
